import React from 'react'

/**
 * 식당 정보를 수정할 때 사용되는 페이지입니다
 * 실제로 수정을 제공하지는 않고 history 테이블 기록을 바탕으로 override하는 방식을 사용합니다.
 * history 테이블에 row가 추가되면 참조 원본 restorant 테이블의 row의 update가 발생(필요한 부분만)
 */
export default function EditPage() {
  return (
    <div></div>
  )
}
